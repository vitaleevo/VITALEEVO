{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo \"cron placeholder - no periodic task required for FastAPI\"",
    "cronSchedule": "0 3 * * *",
    "restartPolicyType": "NEVER"
  }
}
